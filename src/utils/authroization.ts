/**
 * @deprecated This file exists for backward compatibility only.
 * Import from '@/utils/authorization' instead (note the correct spelling).
 */
export { haspermission, hasroutepermission } from './authorization';
export type { UserInstance } from './authorization';
